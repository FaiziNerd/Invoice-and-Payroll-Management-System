/** @deprecated Demo seed removed — data lives in Supabase. Kept for import compatibility. */
export function ensureCompanyBootstrap(_companyId?: string, _companyName?: string): void {}

export function initializeSeedData(_companyId?: string): void {
  ensureCompanyBootstrap(_companyId);
}
